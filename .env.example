# Local dev: leave empty to use Vite proxy (/api → http://localhost:8080).
# Production (Render): set to the deployed backend (see render.yaml).
# VITE_API_URL=http://localhost:8080
# VITE_API_URL=https://cs4135-fomo-backend.onrender.com

VITE_API_URL=
